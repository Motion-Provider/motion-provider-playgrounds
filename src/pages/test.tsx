import { MotionChain_21bf5b } from "@/test/test1";

export default function Test() {
  return (
    <div className="h-screen items-center justify-center flex flex-col w-full">
      <MotionChain_21bf5b />
      <MotionChain_f9ec17 />
    </div>
  );
}

import MotionChain from "@/motion/motion-chain";
import { MotionAnimationProps } from "@/motion/types";

const items = Array.from({ length: 5 }, (_, i) => (
  <div className="size-8 rounded-2xl bg-stone-700 text-white grid place-items-center">
    {i + 1}
  </div>
));
const animations = items.map(() => ({
  mode: ["filterBrightnessFade", "burakPulse"],
  transition: "cubicBounce",
  duration: 2.5,
})) as MotionAnimationProps[];

export function MotionChain_b73242() {
  return (
    <MotionChain
      animations={animations}
      config={{
        duration: 0.15,
        delayLogic: "linear",
      }}
      elementType={"div"}
    >
      {items}
    </MotionChain>
  );
}

/* 
This code is generated by the playground 
engine and matches with the current state 
of the animation you can copy&paste, 
ready-to-use!
*/

const Aitems = Array.from({ length: 5 }, (_, i) => (
  <div className="size-12 my-1 rounded-2xl bg-stone-700 text-white grid place-items-center">
    {i + 1}
  </div>
));
const Aanimations = items.map(() => ({
  mode: ["rotateFlipX", "scaleZoomIn"],
  transition: "cubicBounce",
  duration: 2.5,
})) as MotionAnimationProps[];

export function MotionChain_f9ec17() {
  return (
    <MotionChain
      animations={Aanimations}
      config={{
        duration: 0.15,
        delayLogic: "linear",
      }}
      elementType={"div"}
    >
      {Aitems}
    </MotionChain>
  );
}
