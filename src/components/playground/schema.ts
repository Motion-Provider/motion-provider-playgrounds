import { SchemaProps } from "@/interfaces/@types-components";

export default {
  borderColor: "border-rose-500",
  borderBlur: "blur-none",
  circleCount: 30,
  complexity: 1,
} satisfies SchemaProps;
